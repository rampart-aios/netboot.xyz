name: test_and_build

on:
  pull_request:
    branches: ['**']
  push:
    branches: ['**']

env:
  EFI_FILENAME_PREFIX: "netboot.xyz-rampart-aios"
  ARTIFACT_RETENTION_DAYS: 90
  DOCKER_IMAGE: "localbuild"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: '3.13' # renovate: datasource=github-tags depName=python

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip  
        pip install ansible==10.2.0 # renovate: datasource=pypi
        pip install ansible-lint==24.7.0 # renovate: datasource=pypi

    - name: Syntax Check
      run: |
        ansible-playbook site.yml --syntax-check

    - name: Ansible lint
      run: |
        ansible-lint -v roles/netbootxyz/tasks

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Get commit hash and branch
      id: get_commit
      run: |
        commit_hash=$(git rev-parse --short HEAD)
        branch_name=$(git rev-parse --abbrev-ref HEAD | sed 's/[^a-zA-Z0-9]/-/g')
        echo "commit_hash=$commit_hash" >> $GITHUB_OUTPUT
        echo "branch_name=$branch_name" >> $GITHUB_OUTPUT
        echo "Building for branch: $branch_name, commit: $commit_hash"
    
    - name: Build EFI Bootloader
      id: build
      run: |
        echo "Building EFI bootloader for branch: ${{ steps.get_commit.outputs.branch_name }}, commit: ${{ steps.get_commit.outputs.commit_hash }}"
        
        # Build EFI bootloader
        docker build -t ${{ env.DOCKER_IMAGE }} -f Dockerfile .
        docker run --rm -i -v $(pwd):/buildout ${{ env.DOCKER_IMAGE }}
        
        # Verify EFI content
        echo "Verifying EFI content..."
        ls -la buildout/ipxe/netboot.xyz.efi
        
        # Rename EFI file with branch and commit
        efi_filename="${{ env.EFI_FILENAME_PREFIX }}-${{ steps.get_commit.outputs.branch_name }}-${{ steps.get_commit.outputs.commit_hash }}.efi"
        cp buildout/ipxe/netboot.xyz.efi buildout/ipxe/$efi_filename
        echo "Renamed to $efi_filename"
        
        echo "version=${{ steps.get_commit.outputs.commit_hash }}" >> $GITHUB_OUTPUT
    
    - name: Upload EFI file as artifact
      uses: actions/upload-artifact@v4
      with:
        retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
        name: ${{ env.EFI_FILENAME_PREFIX }}-${{ steps.get_commit.outputs.branch_name }}-${{ steps.get_commit.outputs.version }}.efi
        path: buildout/ipxe/${{ env.EFI_FILENAME_PREFIX }}-${{ steps.get_commit.outputs.branch_name }}-${{ steps.get_commit.outputs.commit_hash }}.efi 